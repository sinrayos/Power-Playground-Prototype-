{
  "name": "power-playground-multiplayer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run",
    "test:remote": "node test/remote-smoke.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
